### Lists ###

my_list = list() 
# Usa el constructor: 
# es más lento y menos "Pythónico" (idiomático), pero útil si necesitas pasar un iterable para inicializar.
my_other_list = []
# Usa la sintaxis literal (corchetes): 
# es la forma más rápida y preferida por la comunidad para crear una lista vacía.

print(len(my_list))

my_list.count(0) # Metodo de lista
# Retorna el numero de ocurrencias de un valor en esta lista

len(my_list) # Funcion incorporada

## test